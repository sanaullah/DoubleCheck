/**
 * The contract the corpus handlers implement.
 *
 * `implements` had a detector in both parsers and zero rows everywhere, because
 * no analysed project declared an interface. That is absence of data, not
 * absence of capability — and the two are only distinguishable if some fixture
 * declares one.
 */
interface {

    function respond( required any payload );

}
