class {

	function find( required string userId ) {
		var apiKey = "seeded-test-key-123456";
		return queryExecute( "SELECT * FROM users WHERE id = #arguments.userId#" );
	}

	function cleanup() {
		try {
			riskyOperation();
		} catch (any error) {

		}
	}
}